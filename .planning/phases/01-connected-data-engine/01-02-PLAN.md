---
phase: 01-connected-data-engine
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/app/api/transactions/route.ts
  - src/app/api/transactions/[id]/route.ts
  - src/app/api/import/confirm/route.ts
  - src/app/api/accounts/route.ts
  - src/components/dashboard/overview.tsx
autonomous: true
requirements: [CALC-01, CALC-02, CALC-03]

must_haves:
  truths:
    - "Adding a transaction via API immediately reflects in account balance on next dashboard load"
    - "Deleting a transaction updates the account balance accordingly"
    - "Importing transactions via CSV/PDF and confirming updates budget spent figures"
    - "Monthly surplus is displayed on the dashboard overview"
    - "Existing data is not corrupted — accounts with no transactions show 0 balance gracefully"
  artifacts:
    - path: "src/app/api/transactions/route.ts"
      provides: "Transaction creation that no longer needs to update Account.balance"
      contains: "POST"
    - path: "src/app/api/import/confirm/route.ts"
      provides: "Import confirmation that creates transactions with accountId linkage"
      contains: "accountId"
    - path: "src/components/dashboard/overview.tsx"
      provides: "Dashboard overview displaying monthlySurplus"
      contains: "monthlySurplus"
  key_links:
    - from: "src/app/api/transactions/route.ts"
      to: "src/lib/db/calculations.ts"
      via: "Transaction creation feeds into computed balance"
      pattern: "prisma\\.transaction\\.create"
    - from: "src/components/dashboard/overview.tsx"
      to: "DashboardOverview.monthlySurplus"
      via: "Renders surplus from server data"
      pattern: "monthlySurplus|surplus"
---

<objective>
Wire the computed calculation layer into all data entry points (transaction CRUD, import) and display the monthly surplus on the dashboard. Ensure no stored balance is manually updated — all values flow from transaction data.

Purpose: Complete the connected data engine by ensuring every user action that changes transaction data is immediately reflected in displayed balances, budgets, and surplus without manual recalculation steps.
Output: Updated API routes and dashboard component that display live-computed values.
</objective>

<execution_context>
@/Users/ibrahimal-tawil/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ibrahimal-tawil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-connected-data-engine/01-01-SUMMARY.md
@src/app/api/transactions/route.ts
@src/app/api/import/confirm/route.ts
@src/components/dashboard/overview.tsx

<interfaces>
From src/lib/db/calculations.ts (created in Plan 01):
```typescript
export async function computeAccountBalance(accountId: string): Promise<number>;
export async function computeAccountBalances(userId: string): Promise<Map<string, number>>;
export async function computeBudgetSpent(userId: string, categoryId: string, month: number, year: number): Promise<number>;
export async function computeAllBudgetSpent(userId: string, month: number, year: number): Promise<Map<string, number>>;
export async function computeMonthlySurplus(userId: string, month: number, year: number): Promise<MonthlySummary>;
```

From src/lib/types.ts (updated in Plan 01):
```typescript
export interface MonthlySummary {
  income: number;
  spending: number;
  surplus: number;
}

export interface DashboardOverview {
  // ... existing fields ...
  monthlySurplus: number;  // NEW: income - spending for the month
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove manual balance updates from transaction and import API routes</name>
  <files>src/app/api/transactions/route.ts, src/app/api/transactions/[id]/route.ts, src/app/api/import/confirm/route.ts, src/app/api/accounts/route.ts</files>
  <action>
Since balances are now computed from transactions (Plan 01), remove any code that manually updates `Account.balance` when transactions are created/deleted/imported.

1. **`src/app/api/transactions/route.ts` (POST handler)**:
   - Check if there is any `prisma.account.update({ data: { balance: ... } })` call after transaction creation. If yes, remove it.
   - The transaction creation itself is sufficient — computed balance will reflect it.
   - If there is no manual balance update (likely the case based on current code), no change needed here. Verify and move on.

2. **`src/app/api/transactions/[id]/route.ts` (DELETE handler)**:
   - Check if transaction deletion triggers an `Account.balance` update. If yes, remove it.
   - Same principle: deleting a transaction automatically changes the computed balance.

3. **`src/app/api/import/confirm/route.ts`**:
   - Check if bulk transaction creation triggers an `Account.balance` update. If yes, remove it.
   - Ensure each imported transaction has `accountId` set when the user selected an account during import. Read the existing code to verify this linkage exists. If transactions are created without accountId, that is OK — they contribute to budget/surplus but not to a specific account balance.

4. **`src/app/api/accounts/route.ts`**:
   - In the account creation handler (POST), the user provides an initial balance. Keep allowing this — but now it serves as a "starting balance" transaction instead. 
   - ALTERNATIVE (simpler, preserving backward compat): Keep the Account.balance field populated on creation for reference, but it will not be used for display (queries.ts already ignores it per Plan 01). This is the preferred approach — no change needed to account creation.
   - Verify no other routes update Account.balance and document findings.

The key principle: We do NOT need to actively remove working code if it updates Account.balance — it is harmless. The critical change was in Plan 01 (queries no longer READ it). However, if any route does `account.balance += amount` style mutations, those should be removed as they are dead code that could confuse future developers.
  </action>
  <verify>
    <automated>cd /Users/ibrahimal-tawil/GitHub/budget && grep -rn "balance" src/app/api/ | grep -v "node_modules" | grep -v ".next" | head -20</automated>
  </verify>
  <done>No API route mutates Account.balance after transaction changes (or if it does, it is documented as legacy and harmless). Transaction creation and import confirmation work without balance side-effects.</done>
</task>

<task type="auto">
  <name>Task 2: Display monthly surplus on dashboard overview and verify budget updates</name>
  <files>src/components/dashboard/overview.tsx, src/app/dashboard/page.tsx</files>
  <action>
1. **`src/components/dashboard/overview.tsx`**:
   - Find where `savedAmount` or savings-related data is displayed.
   - Add a visible "Monthly Surplus" metric card that shows `monthlySurplus` from the overview data.
   - Format as currency using the existing format utilities (check `src/lib/format.ts`).
   - If `monthlySurplus` is positive, show in green. If negative (deficit), show in red/orange.
   - This should be prominent — either replace the existing "Saved" card or add alongside it.
   - Use existing card styling patterns (look at how netWorth, monthlyIncome, monthlySpend are displayed).

2. **`src/app/dashboard/page.tsx`**:
   - Verify that `getDashboardOverview()` is called and its result (including the new `monthlySurplus` field) is passed to DashboardShell/overview component.
   - No changes likely needed here since the data flows through `initialData` — but confirm the type is compatible.

3. **Verify the budget spent display**:
   - The `spending.tsx` component already renders `SpendCategory[]` which includes `amount` (spent) and `budget` (allocated).
   - Since Plan 01 updated `getSpendingData()` to use `computeAllBudgetSpent()`, this should already work.
   - Read `src/components/dashboard/spending.tsx` to confirm it renders budget vs spent — no changes needed if it already shows the comparison.

IMPORTANT: Match existing code style. Use Tailwind classes. Use existing format utilities. Do not add new dependencies.
  </action>
  <verify>
    <automated>cd /Users/ibrahimal-tawil/GitHub/budget && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Dashboard overview displays monthly surplus prominently. TypeScript compiles. Budget spent figures come from computed transactions. The full data pipeline works: transaction added -> next page load shows updated balance, budget spent, and surplus.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds (full Next.js build validates SSR + client components)
- Dashboard overview component references `monthlySurplus` 
- No API route manually updates Account.balance after transaction CRUD (or it is documented as inert legacy)
</verification>

<success_criteria>
- Monthly surplus is visible on the dashboard overview page
- Adding a transaction (manual or import) does not require a separate balance recalculation step — next page load reflects it automatically
- Deleting a transaction similarly updates computed balance without manual intervention  
- Budget spent/remaining in the spending tab reflects actual transactions for the month
- Existing accounts with stored balance values continue to work (graceful fallback to 0 if no transactions)
- Full project builds successfully with `npm run build`
</success_criteria>

<output>
After completion, create `.planning/phases/01-connected-data-engine/01-02-SUMMARY.md`
</output>
